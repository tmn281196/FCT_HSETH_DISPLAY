<UserControl x:Class="Controls.DevicesControl.FND_SegementBuilder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             xmlns:local="clr-namespace:Controls.DevicesControl"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance Type=local:FND_SegementBuilder}"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ControlTemplate x:Key="ButtonControlNonHoverEffect" TargetType="{x:Type Button}">
            <Grid Height="{TemplateBinding Height}">
                <Ellipse x:Name="backgroundEllipse" Width="0" Height="0" Fill="#4F808080" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0" Background="#01000000" SnapsToDevicePixels="True">
                    <ContentPresenter x:Name="contentPresenter" 
                              ContentTemplate="{TemplateBinding ContentTemplate}"
                              Content="{TemplateBinding Content}"
                              ContentStringFormat="{TemplateBinding ContentStringFormat}"
                              Focusable="False"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              Margin="{TemplateBinding Padding}"
                              RecognizesAccessKey="True"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Cursor="Hand"/>
                </Border>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" TargetName="border" Value="{x:Null}"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="{x:Null}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#4F808080"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" TargetName="border" Value="Gray"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="Black"/>
                    <Setter Property="Foreground" Value="#FF838383"/>
                </Trigger>


            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="FlatDataGridHeaderNomal" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                        <Grid Background="{TemplateBinding Background}" HorizontalAlignment="Stretch" >
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#a6a6a6"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="FlatDataGridCell" TargetType="{x:Type DataGridCell}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Grid Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center" Margin="0,0,0,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True" >
                    <Setter Property="Background" Value="#3261AF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FlatRowDataGrid" TargetType="{x:Type DataGridRow}">
            <Style.Triggers>
                <Trigger Property="IsEditing" Value="True">
                    <Setter Property="Background" Value="DarkCyan"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="FlatDataGrid" TargetType="{x:Type DataGrid}">
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Vertical"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#302E2E"/>
            <Setter Property="VerticalGridLinesBrush" Value="#302E2E"/>
            <Setter Property="FontFamily" Value="Calibri"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="ColumnHeaderHeight" Value="25"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="AlternatingRowBackground" Value="#b6b6b6"/>
            <Setter Property="RowBackground" Value="#c6c6c6"/>
            <Setter Property="CanUserSortColumns" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CellStyle" Value="{StaticResource FlatDataGridCell}"/>
            <Setter Property="RowStyle" Value="{StaticResource FlatRowDataGrid}"/>
        </Style>
        <Style x:Key="LabelSegement" TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Label}">
                        <Grid SnapsToDevicePixels="true">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="10"/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="10"/>
                            </Grid.ColumnDefinitions>
                            <Polygon Fill="{TemplateBinding Background}">
                                <Polygon.Points>
                                    <Point X="10" Y="20"/>
                                    <Point X="0" Y="10"/>
                                    <Point X="10" Y="0"/>
                                </Polygon.Points>
                            </Polygon>
                            <Polygon Fill="{TemplateBinding Background}" Grid.Column="2">
                                <Polygon.Points>
                                    <Point X="0" Y="20"/>
                                    <Point X="10" Y="10"/>
                                    <Point X="0" Y="0"/>
                                </Polygon.Points>
                            </Polygon>
                            <Border Background="{TemplateBinding Background}" BorderThickness="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Column="1"/>
                            <ContentPresenter Grid.Column="1" HorizontalAlignment="Center" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="LabelSegementVertical" TargetType="{x:Type Label}">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Label}">
                        <Grid SnapsToDevicePixels="true">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="10"/>
                                <RowDefinition/>
                                <RowDefinition Height="10"/>
                            </Grid.RowDefinitions>
                            <Polygon Fill="{TemplateBinding Background}">
                                <Polygon.Points>
                                    <Point X="20" Y="10"/>
                                    <Point X="10" Y="0"/>
                                    <Point X="0" Y="10"/>
                                </Polygon.Points>
                            </Polygon>
                            <Polygon Fill="{TemplateBinding Background}" Grid.Row="2">
                                <Polygon.Points>
                                    <Point X="20" Y="0"/>
                                    <Point X="10" Y="10"/>
                                    <Point X="0" Y="0"/>
                                </Polygon.Points>
                            </Polygon>
                            <Border Background="{TemplateBinding Background}" BorderThickness="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="1"/>
                            <ContentPresenter Grid.Row="1" HorizontalAlignment="Center" RecognizesAccessKey="True" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- 7-segment editor gets a fixed slot; the Character/Digits table takes all remaining width so its two
             columns stretch to fill (no trailing empty space). -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="210"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition />
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Label Content="{Binding SelectedCharacter.character}" ContentStringFormat=" Character: {0}" FontSize="20" Foreground="Black" Padding="0"
               VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Grid x:Name="gridSegement" Grid.Row="1" Margin="5" Background="#11003a71" Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="20"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="20"/>
                <RowDefinition Height="50"/>
                <RowDefinition Height="20"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Label Grid.Row="1" Grid.Column="2" Margin="-10,0" Background="Green" Style="{StaticResource LabelSegement}" Content="0" Foreground="WhiteSmoke" Padding="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" MouseDown="ToggleSegement_mouseDown"/>
            <Label Grid.Row="2" Grid.Column="1" Margin="0,-10" Background="Green" Style="{StaticResource LabelSegementVertical}" Content="5" Foreground="WhiteSmoke" Padding="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" MouseDown="ToggleSegement_mouseDown"/>
            <Label Grid.Row="2" Grid.Column="3" Margin="0,-10" Background="Green" Style="{StaticResource LabelSegementVertical}" Content="1" Foreground="WhiteSmoke" Padding="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" MouseDown="ToggleSegement_mouseDown"/>
            <Label Grid.Row="3" Grid.Column="2" Margin="-10,0" Background="Green" Style="{StaticResource LabelSegement}" Content="6" Foreground="WhiteSmoke" Padding="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" MouseDown="ToggleSegement_mouseDown"/>
            <Label Grid.Row="4" Grid.Column="1" Margin="0,-10" Background="Green" Style="{StaticResource LabelSegementVertical}" Content="4" Foreground="WhiteSmoke" Padding="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" MouseDown="ToggleSegement_mouseDown"/>
            <Label Grid.Row="4" Grid.Column="3" Margin="0,-10" Background="Green" Style="{StaticResource LabelSegementVertical}" Content="2" Foreground="WhiteSmoke" Padding="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" MouseDown="ToggleSegement_mouseDown"/>
            <Label Grid.Row="5" Grid.Column="2" Margin="-10,0" Background="Green" Style="{StaticResource LabelSegement}" Content="3" Foreground="WhiteSmoke" Padding="0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" MouseDown="ToggleSegement_mouseDown"/>
        </Grid>

        <Border Grid.Column="1" Grid.RowSpan="2" Background="#d6d6d6" CornerRadius="0" Margin="5">
            <DataGrid x:Name="dgrSEGLOOKUP" Background="Transparent" Style="{StaticResource FlatDataGrid}" BorderThickness="0" Margin="5" AutoGenerateColumns="False"
                      SelectionChanged="dgrSEGLOOKUP_SelectionChanged" ColumnHeaderStyle="{StaticResource ResourceKey=FlatDataGridHeaderNomal}" RowHeight="20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Character" Binding="{Binding character}" Width="0.7*">

                    </DataGridTextColumn>
                    <DataGridTextColumn Header="Digits" Binding="{Binding digitString}" IsReadOnly="True" Width="1*">

                    </DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="15,0">
            <Button x:Name="btOpenModel"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Template="{DynamicResource ButtonControlNonHoverEffect}"
                        Height="30"  Width="120"
                        BorderThickness="0" 
                        Foreground="#FF109322" 
                        FontSize="14" 
                        Margin="0,5,0,5" 
                        Padding="5" 
                        Background="{x:Null}"
                        Click="btOpenModel_Click">

                <Grid HorizontalAlignment="Left" Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <fa:IconImage Icon="FolderOpen" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#ffa000"/>
                    <Label Content="Open Define" Margin="5,0,0,0" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="Black" Padding="0" Grid.Column="1" FontStretch="UltraExpanded"/>
                </Grid>
            </Button>
            <Button x:Name="btSaveAsModel"
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Center"
                        Template="{DynamicResource ButtonControlNonHoverEffect}"
                        Height="30"  Width="130"
                        BorderThickness="0" 
                        Foreground="#FF109322" 
                        FontSize="14" 
                        Margin="0,5,0,5" 
                        Padding="5" 
                        Background="{x:Null}"
                        Click="btSaveAsModel_Click">

                <Grid HorizontalAlignment="Left" Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <fa:IconImage  Icon="Save" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#ffa000"/>
                    <Label Content="Save Define As" Margin="5,0,0,0" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" Foreground="Black" Padding="0" Grid.Column="1" FontStretch="UltraExpanded"/>
                </Grid>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
