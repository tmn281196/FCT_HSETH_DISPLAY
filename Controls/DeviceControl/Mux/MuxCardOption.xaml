<UserControl x:Class="Controls.MuxCardOption"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Controls"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="FlatDataGridCell" TargetType="{x:Type DataGridCell}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridCell}">
                        <Grid Background="{TemplateBinding Background}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>

            </Setter>
            <Setter Property="FontWeight" Value="DemiBold"/>
            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True" >
                    <Setter Property="Background" Value="#3261AF"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="FlatDataGridHeaderNomal" TargetType="{x:Type DataGridColumnHeader}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type DataGridColumnHeader}">
                        <Grid Background="{TemplateBinding Background}" HorizontalAlignment="Stretch" >
                            <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10,0"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#151515"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style x:Key="FlatDataGrid" TargetType="{x:Type DataGrid}">
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="Vertical"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#302E2E"/>
            <Setter Property="VerticalGridLinesBrush" Value="#302E2E"/>
            <Setter Property="FontFamily" Value="Calibri"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="ColumnHeaderHeight" Value="25"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="AlternationCount" Value="2"/>
            <Setter Property="AlternatingRowBackground" Value="#FF444444"/>
            <Setter Property="RowBackground" Value="#AF444444"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="CanUserSortColumns" Value="False"/>
            <Setter Property="CanUserReorderColumns" Value="False"/>
            <Setter Property="CellStyle" Value="{StaticResource FlatDataGridCell}"/>
        </Style>
        <ControlTemplate x:Key="ButtonControlNonHoverEffect" TargetType="{x:Type Button}">
            <Grid>
                <Ellipse x:Name="backgroundEllipse" Width="0" Height="0" Fill="#4F808080" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Ellipse x:Name="backgroundHoverEllipse" Width="0" Height="0" Fill="#4F808080" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="#01000000" SnapsToDevicePixels="True">
                    <ContentPresenter x:Name="contentPresenter" 
                              ContentTemplate="{TemplateBinding ContentTemplate}"
                              Content="{TemplateBinding Content}"
                              ContentStringFormat="{TemplateBinding ContentStringFormat}"
                              Focusable="False"
                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                              Margin="{TemplateBinding Padding}"
                              RecognizesAccessKey="True"
                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                              Cursor="Hand"/>
                </Border>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" TargetName="border" Value="{x:Null}"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="{x:Null}"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" TargetName="border" Value="#4F808080"/>
                </Trigger>
                <EventTrigger RoutedEvent="FrameworkElement.MouseLeave">
                    <EventTrigger.Actions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Duration="0:0:1" To="0" SpeedRatio="3"
                                             Storyboard.TargetName="backgroundEllipse"
                                             Storyboard.TargetProperty="Width" >
                                </DoubleAnimation>
                                <DoubleAnimation Duration="0:0:1" To="0" SpeedRatio="3"
                                             Storyboard.TargetName="backgroundEllipse"
                                             Storyboard.TargetProperty="Height" >
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger.Actions>
                </EventTrigger>
                <EventTrigger RoutedEvent="Button.Click">
                    <EventTrigger.Actions>
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Duration="0:0:1" From="0" To="100" SpeedRatio="5"
                                             DecelerationRatio="0.1"
                                             Storyboard.TargetName="backgroundEllipse"
                                             Storyboard.TargetProperty="Width" >
                                </DoubleAnimation>
                                <DoubleAnimation Duration="0:0:1" From="0" To="100" SpeedRatio="5"
                                             AccelerationRatio="0.1"
                                             Storyboard.TargetName="backgroundEllipse"
                                             Storyboard.TargetProperty="Height" >
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger.Actions>
                </EventTrigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" TargetName="border" Value="Gray"/>
                    <Setter Property="BorderBrush" TargetName="border" Value="#FFADB2B5"/>
                    <Setter Property="Foreground" Value="#FF838383"/>
                </Trigger>


            </ControlTemplate.Triggers>
        </ControlTemplate>

    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.6*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Border Grid.Column="0" Grid.RowSpan="2" Background="#11ffffff" Margin="5" VerticalAlignment="Stretch"  CornerRadius="0">
            <Grid Margin="3,3,3,3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Label Content="MUX CHANNELs DATAs" VerticalAlignment="Top" HorizontalAlignment="Left"
                    Foreground="White" FontWeight="Bold" FontSize="12"/>
                <Grid Grid.Row="1" Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <DataGrid x:Name="MUX_Channels_Table1"  ItemsSource="{Binding ChanelsEdittingPart1}"
                              Style="{StaticResource FlatDataGrid}"
                              CellStyle="{StaticResource FlatDataGridCell}"
                              ColumnHeaderStyle="{StaticResource FlatDataGridHeaderNomal}" BorderThickness="0"
                            AutoGenerateColumns="False" Margin="0" Width="Auto" HeadersVisibility="Column"
                            GridLinesVisibility="All" CanUserSortColumns="False" CanUserAddRows="False" VerticalScrollBarVisibility="Disabled"
                            SelectionMode="Extended" HorizontalAlignment="Left" VerticalAlignment="Top"
                            VerticalContentAlignment="Center"
                            SelectionUnit="Cell" ColumnHeaderHeight="25">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Height" Value="18"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Channel_P}" Header="Ch+" Width="50" IsReadOnly="True"
                                                                        FontSize="11" >
                                <DataGridTextColumn.CellStyle >
                                    <Style BasedOn="{StaticResource FlatDataGridCell}" TargetType="DataGridCell">
                                        <Setter Property="Foreground" Value="Cyan"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=IsUse, UpdateSourceTrigger=PropertyChanged}" Value="false">
                                                <Setter Property="Background" Value="DarkGray"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding Channel_N}" Header="Ch-" Width="40" FontSize="11"/>
                        </DataGrid.Columns>
                    </DataGrid>

                    <DataGrid x:Name="MUX_Channels_Table2"  Grid.Column="1" ItemsSource="{Binding ChanelsEdittingPart2}"  
                              CellStyle="{StaticResource FlatDataGridCell}"
                              Style="{StaticResource FlatDataGrid}"
                                      ColumnHeaderStyle="{StaticResource FlatDataGridHeaderNomal}" BorderThickness="0"
                                    AutoGenerateColumns="False" Margin="0" Width="Auto" HeadersVisibility="Column"
                                    GridLinesVisibility="All" CanUserSortColumns="False"  CanUserAddRows="False" VerticalScrollBarVisibility="Disabled"
                                    SelectionMode="Extended" HorizontalAlignment="Left" VerticalAlignment="Top"
                                    VerticalContentAlignment="Center"
                                    SelectionUnit="Cell" ColumnHeaderHeight="25">
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="Height" Value="18"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Channel_P}" Header="Ch+" Width="50" IsReadOnly="True"
                                                                        FontSize="11">
                                <DataGridTextColumn.CellStyle >
                                    <Style BasedOn="{StaticResource FlatDataGridCell}" TargetType="DataGridCell">
                                        <Setter Property="Foreground" Value="Cyan"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Path=IsUse, UpdateSourceTrigger=PropertyChanged}" Value="false">
                                                <Setter Property="Background" Value="DarkGray"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </DataGridTextColumn.CellStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Binding="{Binding Channel_N}" Header="Ch-" Width="40" FontSize="11"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Grid>
        </Border>
        <Border Grid.Column="1" Grid.Row="0" Grid.RowSpan="2" Background="#11ffffff" Margin="5" VerticalAlignment="Top"  CornerRadius="0">
            <Grid Margin="3,3,3,3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Label Content="MUX CHANNELs SELECT" VerticalAlignment="Top" HorizontalAlignment="Left"
                    Foreground="White" FontWeight="Bold" FontSize="12"/>
                <Button Content="Select All" Template="{StaticResource ButtonControlNonHoverEffect}" Click="SellectAll_Click"
                        Background="Black" Foreground="White" BorderThickness="0,0,0,2" Width="75" FontWeight="Bold"
                        Margin="0,2,75,2" Padding="10,0"  HorizontalAlignment="Right"></Button>
                <Button Content="Clear All" Template="{StaticResource ButtonControlNonHoverEffect}" Click="DeleteAll_Click"
                        Background="Black" Foreground="White" BorderThickness="0,0,0,2" Width="75" FontWeight="Bold"
                        Margin="0,2,0,2" Padding="10,0"  HorizontalAlignment="Right"></Button>
                
                <WrapPanel x:Name="MuxChannelsPanel" Grid.Row="1" ItemHeight="30" ItemWidth="70" VerticalAlignment="Center" Width="600"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
