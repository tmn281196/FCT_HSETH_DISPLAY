<UserControl
    x:Class="Controls.CustomControls.IntegerUpDown"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Controls.CustomControls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    mc:Ignorable="d">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="15" MaxWidth="20" />
        </Grid.ColumnDefinitions>
        <TextBox
            x:Name="txtNum"
            Grid.RowSpan="2"
            Width="60"
            Margin="0"
            HorizontalAlignment="Stretch"
            x:FieldModifier="private"
            Text="0"
            TextAlignment="Center"
            TextChanged="txtNum_TextChanged" />
        <Button
            x:Name="cmdUp"
            Grid.Column="1"
            Width="15"
            Padding="0,-4,0,0"
            x:FieldModifier="private"
            Click="cmdUp_Click"
            Content="▲"
            FontSize="10" />
        <Button
            x:Name="cmdDown"
            Grid.Row="1"
            Grid.Column="1"
            Width="15"
            Padding="0,-4,0,0"
            x:FieldModifier="private"
            Click="cmdDown_Click"
            Content="▼"
            FontSize="10" />
    </Grid>
</UserControl>